name: Temporary set Mobile internal version 0.1.5

on:
  push:
    branches:
      - mobile-app-v1-build
    paths:
      - '.github/workflows/tmp-set-mobile-version-v015.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set exact internal app version
        shell: bash
        run: |
          set -euo pipefail
          FILE=app/build.gradle
          python3 - <<'PY'
          from pathlib import Path
          p = Path('app/build.gradle')
          s = p.read_text()
          old = "def buildVersionName = System.getenv('DEZGRE_VERSION_NAME') ?: '0.1.1'"
          new = "def buildVersionName = '0.1.5'"
          if s.count(old) != 1:
              raise SystemExit('Expected one versionName declaration')
          p.write_text(s.replace(old, new, 1))
          PY
          grep -q "def buildVersionName = '0.1.5'" "$FILE"
      - name: Commit only Gradle version
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add app/build.gradle
          git commit -m 'build(mobile): set internal version 0.1.5'
          git push origin HEAD:mobile-app-v1-build
