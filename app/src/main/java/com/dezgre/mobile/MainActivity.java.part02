                    @Override public void run() {
                        if (error.status == 401 || error.status == 403) {
                            tokenStore.clear();
                            bearer = null;
                            showLogin("Tu sesión expiró. Inicia sesión nuevamente.");
                        } else {
                            showLogin("No se pudo validar la sesión: " + error.getMessage());
                        }
                    }
                });
            }
        });
    }

    private View loadingScreen(String message) {
        LinearLayout wrap = new LinearLayout(this);
        wrap.setOrientation(LinearLayout.VERTICAL);
        wrap.setGravity(Gravity.CENTER);
        wrap.setPadding(dp(28), dp(28), dp(28), dp(28));
        wrap.setBackgroundColor(BG);
        ImageView logo = new ImageView(this);
        logo.setImageResource(R.drawable.ic_launcher);
        logo.setScaleType(ImageView.ScaleType.CENTER_INSIDE);
        wrap.addView(logo, new LinearLayout.LayoutParams(dp(70), dp(70)));
        wrap.addView(gap(18));
        ProgressBar progress = new ProgressBar(this);
        wrap.addView(progress, new LinearLayout.LayoutParams(dp(34), dp(34)));
        wrap.addView(gap(14));
        TextView label = text(message, 14, MUTED, false);
        label.setGravity(Gravity.CENTER);
        wrap.addView(label);
        return wrap;
    }

    private void showLogin(String initialError) {
        bearer = null;
        currentUserName = "Usuario";
        currentUserRole = "";
        currentStoreName = "Tienda";
        final ScrollView scroll = new ScrollView(this);
        scroll.setFillViewport(true);
        scroll.setBackgroundColor(BG);
        LinearLayout wrap = new LinearLayout(this);
        wrap.setOrientation(LinearLayout.VERTICAL);
        wrap.setGravity(Gravity.CENTER_HORIZONTAL);
        wrap.setPadding(dp(22), dp(36), dp(22), dp(32));
        scroll.addView(wrap);

        ImageView logo = new ImageView(this);
        logo.setImageResource(R.drawable.ic_launcher);
        logo.setScaleType(ImageView.ScaleType.CENTER_INSIDE);
        wrap.addView(logo, new LinearLayout.LayoutParams(dp(88), dp(88)));
        wrap.addView(gap(16));
        TextView brand = eyebrow("DEZGRE MOBILE");
        brand.setGravity(Gravity.CENTER);
        wrap.addView(brand);
        wrap.addView(gap(8));
        TextView heading = text("Accede a tu operación", 30, TEXT, true);
        heading.setGravity(Gravity.CENTER);
        wrap.addView(heading);
        wrap.addView(gap(7));
        TextView intro = text("La misma cuenta de DEZGRE, ahora en una experiencia móvil conectada solo a API V1.", 14, MUTED, false);
        intro.setGravity(Gravity.CENTER);
        wrap.addView(intro);
        wrap.addView(gap(26));

        LinearLayout loginCard = card();
        loginCard.setPadding(dp(18), dp(20), dp(18), dp(18));
        wrap.addView(loginCard);
        loginCard.addView(text("Iniciar sesión", 20, TEXT, true));
        loginCard.addView(gap(5));
        loginCard.addView(text("Ingresa con tu correo y contraseña.", 13, MUTED, false));
        loginCard.addView(gap(16));
        final EditText email = new EditText(this);
        email.setHint("Correo electrónico");
        email.setTextColor(TEXT);
        email.setHintTextColor(MUTED);
        email.setSingleLine(true);
        email.setInputType(InputType.TYPE_CLASS_TEXT | InputType.TYPE_TEXT_VARIATION_EMAIL_ADDRESS);
        email.setPadding(dp(14), dp(4), dp(14), dp(4));
        email.setBackground(rounded(Color.rgb(250, 250, 251), BORDER, 9));
        loginCard.addView(email, new LinearLayout.LayoutParams(ViewGroup.LayoutParams.MATCH_PARENT, dp(52)));
        loginCard.addView(gap(10));
        final EditText password = new EditText(this);
        password.setHint("Contraseña");
        password.setTextColor(TEXT);
        password.setHintTextColor(MUTED);
        password.setSingleLine(true);
        password.setInputType(InputType.TYPE_CLASS_TEXT | InputType.TYPE_TEXT_VARIATION_PASSWORD);
        password.setPadding(dp(14), dp(4), dp(14), dp(4));
        password.setBackground(rounded(Color.rgb(250, 250, 251), BORDER, 9));
        loginCard.addView(password, new LinearLayout.LayoutParams(ViewGroup.LayoutParams.MATCH_PARENT, dp(52)));
        loginCard.addView(gap(12));

        final TextView errorText = text(initialError == null ? "" : initialError, 13, DANGER, false);
        errorText.setVisibility(initialError == null ? View.GONE : View.VISIBLE);
        loginCard.addView(errorText);
        if (initialError != null) loginCard.addView(gap(8));

        final Button submit = button("Entrar", true);
        loginCard.addView(submit, new LinearLayout.LayoutParams(ViewGroup.LayoutParams.MATCH_PARENT, dp(50)));
        loginCard.addView(gap(14));
        TextView privacy = text("La contraseña no se guarda. Tu sesión queda protegida con Android Keystore.", 12, MUTED, false);
        loginCard.addView(privacy);

        submit.setOnClickListener(new View.OnClickListener() {
            @Override public void onClick(View v) {
                String emailValue = email.getText().toString().trim();
                String passwordValue = password.getText().toString();
                if (emailValue.isEmpty() || passwordValue.isEmpty()) {
                    errorText.setText("Completa correo y contraseña.");
                    errorText.setVisibility(View.VISIBLE);
                    return;
                }
                hideKeyboard();
                submit.setEnabled(false);
                submit.setText("Ingresando…");
                errorText.setVisibility(View.GONE);
                JSONObject body = new JSONObject();
                try {
